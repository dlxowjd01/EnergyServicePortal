<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="WeatherDao">
    <select id="getWeatherIconMonthly" parameterType="Map" resultType="java.util.LinkedHashMap">
        SELECT DATE_FORMAT(time, '%Y%m%d') AS time, icon
        FROM tb_daily_weather
        WHERE site_id = #{site_id}
          AND YEAR(time) = YEAR(NOW())
          AND MONTH(time) = MONTH(NOW())
        ORDER BY YEAR(time), MONTH(time);
    </select>
</mapper>